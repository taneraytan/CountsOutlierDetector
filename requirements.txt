# Direct dependencies (floor pins). For reproducible installs use the fully
# pinned lockfile instead:  pip install -r requirements.lock
streamlit>=1.32
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13
altair>=5.0
openpyxl>=3.1
pyarrow>=14.0
